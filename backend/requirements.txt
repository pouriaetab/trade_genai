# Backend web layer (imports the genai_trader library at repo root)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Domain library deps (also in ../requirements.txt)
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0
matplotlib>=3.7
